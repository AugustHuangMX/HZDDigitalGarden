#fleeting #Econ/metrics #machine_learning

K-means 聚类（K-均值聚类）是一种非监督学习算法，用于将无标签数据划分为 K 个不同的簇。该算法通过迭代过程，将每个数据点分配到最近的质心（中心），并不断更新质心位置，直至簇内距离平方和最小化，最终实现数据的自动归类。该方法常用于市场细分、图像处理和数据挖掘.

在实操过程中，需要多少组，就设置多少个 $k$。比如你需要设置 3 组（高、中、低），那就令 $k=3$。

---

- [[Non-Supervising Machine Learning]]。