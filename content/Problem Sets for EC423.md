# Problem Set 1

1. Consider the one-period choice problem of a worker endowed with unearned income $y$ and $T$ hours of time. The worker faces wage $w$. The worker's preferences can be described by the utility function
$$U(c, l) = c^\alpha l$$
where $c$ represents consumption of a composite good with a fixed price of 1 and $l$ represents leisure.



(a) Derive the worker's labor supply function and the uncompensated and compensated wage elasticities. Compute the desired hours of work $h^*$ for the case where $T = 80, y = 0, \alpha = 1$.

(b) Consider a worker supplying the desired hours of work $h^*$. The firm wants the worker to supply overtime hours $h'$ at wage $w' > w$ in addition to the first $h^*$ hours remunerated at wage $w$. Derive the overtime labor supply elasticity at $h' = 0$. How does this elasticity relate to the elasticities of the standard hours supply function in (a)? Think of the case where $w'$ is close to $w$.

(c) Using the parameters in (a), derive the overtime premium $w'/w$ necessary to induce the worker to supply $h' = h^*/4$ hours of overtime.




## Question 2

$\max c^s(k)$, $k^{\text{Golden Rule}}$ ,

$$
f'(k^G) - \delta = 0 \implies f'(k^G) = \delta
$$


### （C）

$$
u'(C^*) = \beta u'(C^*)(f'(k^*) + 1 - \delta)
$$

$$
\frac{1}{\beta} = f'(k^*) + 1 - \delta
$$

We then rearrange this:

$$
f'(k^*) = \frac{1}{\beta} - 1 + \delta
$$



## Question 3

Consider the following linear dynamical system:
$$x_{t+1} = (1 - \alpha)x_t + \lambda(1 - x_t),$$
where $0 < \alpha < 1$, $0 < \lambda < 1$, and $0 \le x_0 \le 1$.

This type of dynamical system can arise in many economic situations of interest. For example, some models of labor market dynamics produce this type of equation for the evolution of the unemployment rate. In this context $x_t$ would be the unemployment rate in period $t$, $\alpha$ would represent the fraction of unemployed workers that find jobs in each period, and $\lambda$ would represent the fraction of employed workers that lose their jobs each period.

(a) Show that $0 \le x_t \le 1$ for all $t$.

(b) Determine how many steady states the above equation has. Solve for these steady states in terms of the parameters $\alpha$ and $\lambda$.

(c) Show that starting from any $x_0$ that the sequence of values for $x_t$ will converge to a steady state value. 

*Answer*: 